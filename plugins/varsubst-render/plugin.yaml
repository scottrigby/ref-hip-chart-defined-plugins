apiVersion: v1
name: varsubst-render
version: 0.1.2
runtime: extism/v1
type: render/v1
description: Variable substitution render plugin (placeholder for real Pkl)

config:
  patterns:
    - "templates/*.pkl"
