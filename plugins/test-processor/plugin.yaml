apiVersion: v1
name: test-processor
version: 0.1.0
description: A test render/v1 plugin that processes .test files and reports what it received
runtime: extism/v1
type: render/v1
config:
  patterns:
    - "templates/*.test"
