# Build artifacts
bin/
dist/
plugins/*/plugin.wasm
charts/*/plugin.wasm

# Helm binary and build directory (auto-fetched from fork by Makefile)
helm
.helm-build/

# Chart.lock for container-test-chart is gitignored because it uses a local OCI
# registry (host.containers.internal:5001) whose digests change on each rebuild.
# Chart.lock files for charts that reference GHCR are committed for reproducible builds.
charts/container-test-chart/Chart.lock

# Temporary files
*.tmp

# Mock ArtifactHub server binary
mock-artifacthub/mock-server

# GH Actions setup files
keyring.base64
public.asc
