apiVersion: v3
name: fallback-test-chart
version: 1.0.0
description: Test chart for verifying fallback to globally installed plugin path

# This chart specifies version 0.1.0, but we'll install the plugin
# to the non-versioned path to test that the fallback works
plugins:
  - name: globally-installed-fallback
    version: 0.1.0
    type: render/v1
    repository: oci://127.0.0.1:5001/plugins/globally-installed-fallback
